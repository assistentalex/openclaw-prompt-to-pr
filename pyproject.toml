[project]
name = "prompt-to-pr"
version = "1.6.2"
description = "Full AI-assisted development workflow — from prompt to ready-to-merge PR"
requires-python = ">=3.8"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]